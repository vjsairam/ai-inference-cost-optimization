id: t0-managed-classification-v1
treatment: t0-managed-baseline
workload: classification
provider_mode: managed
stream: true
concurrency: 4
requests: 300
temperature: 0
max_tokens: 64
data_class: public
quality_tier: premium
structured_output_mode: baseline-portable
warmup_requests: 10
repeats: 3
dataset: benchmark/datasets/synthetic/classification-v1
slo_config: config/slo.example.yaml
slo_cell: WL-01/premium
pricing_config: config/providers.example.yaml
timeout_config: policy/treatments/t0-managed-only.yaml
cost_config: config/cost.example.yaml
model: managed-premium
publishable: true
economic_views: [view_a, view_b]
pricing_version: "2026-08-15"
bootstrap_iterations: 2000
seed: 20260815
request_rate_schedule: closed-loop
sampling_parameters:
  token_profile: short
notes: >-
  T0 managed-only baseline. Run the harness in-cluster in benchmark-jobs on the CPU/system
  node group through the gateway ClusterIP. The managed provider must be the real Anthropic API;
  fallback must be disabled for this baseline. Exclude ten warm-up requests before each measured
  repeat. Use the frozen classification-v1 dataset and preserve the provider internet transit in
  the manifest.
