replicaCount: 1

image:
  repository: ghcr.io/example/inference-gateway
  tag: "0.1.0"
  digest: ""
  pullPolicy: IfNotPresent

resources:
  requests:
    cpu: 500m
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 1Gi

serviceMonitor:
  enabled: true
  interval: 15s

